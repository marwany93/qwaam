# Dependencies
node_modules/
.npm-cache/

# Next.js Build Output
.next/
out/
build/

# TypeScript
tsconfig.tsbuildinfo

# AI / Agent tooling
.claude/
.agent/

# Environment Variables (Secrets — NEVER commit these!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Firebase service account key (Secrets — NEVER commit!)
firebase-key.json
*.json.key

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Generated reports & temporary files
lighthouse-report.json
*_REPORT.md
_bmad*/

# Documentation / memory — LOCAL ONLY, never pushed (single-machine memory workflow)
# CLAUDE.md, STATE.md, docs/, SRS.md, README.md, instructions.* all stay on disk only.
*.md
docs/
instructions.md
instructions.txt

# OS files
.DS_Store
Thumbs.db

# Playwright
playwright-report/
test-results/
project.zip
# Saved auth storageState from global-setup (contains session cookies)
e2e/.auth/
