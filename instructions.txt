Act as a Senior Fullstack Engineer. We have a bug where the "Forget Password" email contains a link hardcoded to `http://localhost:3000`, causing a connection failure on production viewports (as seen in WhatsApp Image 2026-06-04 at 19.45.17.jpeg).

Task 1: Locate the Forget Password Logic
- Scan the authentication actions or components (check `src/app/[locale]/(public)/login/page.tsx`, `src/actions/client-actions.ts`, or wherever the password reset email/action is triggered).
- Find where the reset link is being constructed.

Task 2: Make the Domain Dynamic
- Replace any hardcoded `http://localhost:3000` strings with an environment variable: `process.env.NEXT_PUBLIC_APP_URL`.
- If the link is constructed inside a Server Action, you can alternatively derive it dynamically using Next.js headers:
```ts
  import { headers } from 'next/headers';
  const host = (await headers()).get('host');
  const protocol = host?.includes('localhost') ? 'http' : 'https';
  const resetLink = `${protocol}://${host}/${locale}/reset-password?token=...`;

Choose the safest dynamic method based on how our Firebase Auth / Resend reset flow is built.

Task 3: Sync Environment Variables Documentation

    Add a note in CLAUDE.md under System Architecture / Auth Flow stating that NEXT_PUBLIC_APP_URL is a required environment variable for handling deep-links like password resets.

Verify that the build is clean (npx tsc --noEmit) and commit the fix to main.